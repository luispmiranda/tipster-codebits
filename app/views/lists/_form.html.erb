<%= form_for(@list) do |f| %>
  <!--<%# if @list.errors.any? %>
    <div id="error_explanation">
      <h2><%#= pluralize(@list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <%# @list.errors.full_messages.each do |msg| %>
        <li><%#= msg %></li>
      <%# end %>
      </ul>
    </div>
  <%# end %>-->

  <div class="field">
    <%= f.label :category %>
    <%= collection_select :list, :category_id, Category.all, :id, :name %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :desc, 'Description' %>
    <%= f.text_area :desc %>

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
